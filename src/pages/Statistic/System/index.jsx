import React from 'react';

import { PageHeader } from '../../../components/common';

const SystemStatistic = () => {
  return (
    <div className="d-flex flex-column gap-2">
      <PageHeader pageName="Thống kê tương tác hệ thống" />
    </div>
  );
};

export default SystemStatistic;
